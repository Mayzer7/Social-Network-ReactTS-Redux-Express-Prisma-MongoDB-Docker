server {
    listen 80;
    server_name localhost;

    location / {
        root   /usr/share/nginx/html; # Путь к папке с собранными статическими файлами 
        index index.html index.html;
        try_files $uri $uri/ /index.html = 404;
    }
}